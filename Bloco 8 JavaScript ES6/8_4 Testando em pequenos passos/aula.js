
let primoUntil = (num) => {
    for (let i = 0; i < num; i++){
        if i
    }
}



const ePrimo = (num) => {
    let arr = [ ];
    for(let i = 2; i < num; i++)
      if(num % i === 0) {
          return `O número --${num}-- não é primo!`
      }
      arr.push[num]
    return `O número --${num}-- é primo sim senhor! ${arr}`;
  }

  console.log(ePrimo(11))